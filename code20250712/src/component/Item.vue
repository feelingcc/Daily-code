<!-- <script setup>
const { x } = defineProps(['x'])
const emit = defineEmits(['handleClickAdd'])

const onAdd = () => {
    emit('handleClickAdd', 3)
}
</script>

<template>
    <span>{{ x }}</span>
    <button @click="onAdd">+3</button>
</template> -->
